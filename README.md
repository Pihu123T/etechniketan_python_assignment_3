What I did
Answered multiple choice questions on Python basics — lambda functions, modules, imports, and exceptions (assi3.md).
Wrote a function introduce() that prints a name and age, with age as an optional argument.
Wrote a function drop_minimum() that takes multiple numbers and removes the smallest one from the list.
Wrote a function find_max() to find the largest of three numbers using Python's built-in max().
Wrote two lambda functions — one to add two numbers, another to convert Celsius to Fahrenheit.
Created a text file (student.txt), wrote lines into it, and handled errors for cases where the file already exists.
Read the file's content two ways — all at once, and line by line.
Counted the total number of words in the file.
Appended a new line to the existing file.
Created a list of numbers and used exception handling to catch invalid index errors.
Built a calculator.py module with basic math functions (add, subtract, multiply, divide) and imported it into main.py to use them.
Files
File	Description
assi3.md	Answers to the multiple choice questions
a3.py	Solutions to the coding questions (functions, file handling, exceptions, module usage)
student.txt	Text file created and modified during the file-handling tasks
Tech Used

Python 3
